import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF4CD964);
  
  // Text
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textLight = Color(0xFF999999);
  
  // Background
  static const Color background = Colors.white;
  static const Color surface = Colors.white;
  
  // UI Elements
  static const Color border = Color(0xFFEEEEEE);
  static const Color divider = Color(0xFFEEEEEE);
  
  // Selection
  static const Color selected = Colors.black;
  static const Color unselected = Colors.white;
  
  // Actions
  static const Color confirm = Color(0xFF4CD964);
  static const Color cancel = Color(0xFFAAAAAA);
}